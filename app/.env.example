# App
NEXT_PUBLIC_PHOENIX_API_URL=https://perp-api.phoenix.trade
NEXT_PUBLIC_SOLANA_RPC=https://api.mainnet-beta.solana.com
NEXT_PUBLIC_PROGRAM_ID=PhxVau1t1111111111111111111111111111111111
NEXT_PUBLIC_PLATFORM_TREASURY=your-platform-treasury-pubkey
NEXT_PUBLIC_FLIGHT_BUILDER=your-flight-builder-pubkey

# Server — pool registry (either Upstash Redis OR Postgres; Redis preferred)
UPSTASH_REDIS_REST_URL=https://your-db.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-rest-token
# Optional: Postgres remains supported for NAV/leaderboard analytics.
DATABASE_URL=postgresql://user:pass@host/db?sslmode=require
JWT_SECRET=generate-a-long-random-secret
CRON_SECRET=generate-another-random-secret

# Phoenix Flight (register once on mainnet)
FLIGHT_FEE_BPS=5

# Instant withdrawal relayer
# Generate once with `node scripts/gen-relayer-keypair.mjs` and store the SECRET_KEY
# securely. Manager wallets will SPL-approve PUBKEY as delegate for instant refunds.
WITHDRAWAL_RELAYER_PUBKEY=
WITHDRAWAL_RELAYER_SECRET_KEY=
NEXT_PUBLIC_WITHDRAWAL_RELAYER_PUBKEY=
